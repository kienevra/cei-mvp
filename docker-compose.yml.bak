version: '3.8'

services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    env_file:
      - .env
    environment:
      - DATABASE_URL
      - JWT_SECRET
    ports:
      - "8000:8000"
